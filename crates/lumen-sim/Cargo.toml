[package]
name = "lumen-sim"
description = "Simulated HAL, whole-mesh scenario harness and deterministic replay for Lumen"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
authors.workspace = true

[dev-dependencies]
# The mesh scenarios build real datagrams, which needs the codec.
lumen-proto = { version = "0.1.0", path = "../../../lumen-core/crates/lumen-proto" }

[dependencies]
lumen-device = { workspace = true }
lumen-hal = { workspace = true }
